// routes/add.tsx

/** @jsx h */
import {h} from "preact";
import {tw} from "@twind";

export default function Page(){
    return (
        <main class={tw`mt-10`}>
            This is the ADD page.
            hello
        </main>
    );
}